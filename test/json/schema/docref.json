{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://huri.net/schema/docref.json",
  "title": "DocRef",
  "type": "object",
  "required": ["idref"],
  "properties":
  {
    "idref": {"type": "string", "format": "uuid", "description":"UUID of target document"},
    "id": {"type": "string", "format": "uuid", "description":"Optional UUID to refer to this docref"}
  }
}